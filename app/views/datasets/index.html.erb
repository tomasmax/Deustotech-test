<p id="notice"><%= notice %></p>

<h1>Listing Datasets</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Object name</th>
      <th>License title</th>
      <th>License url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @datasets.each do |dataset| %>
      <tr>
        <td><%= dataset.name %></td>
        <td><%= dataset.object_name %></td>
        <td><%= dataset.license_title %></td>
        <td><%= dataset.license_url %></td>
        <td><%= link_to 'Show', dataset %></td>
        <td><%= link_to 'Edit', edit_dataset_path(dataset) %></td>
        <td><%= link_to 'Destroy', dataset, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dataset', new_dataset_path %>
